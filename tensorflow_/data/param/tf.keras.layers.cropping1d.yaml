constraints:
  cropping:
    default: (1, 1)
    dtype: int
    structure:
      - integer
      - tuple
    shape: 2
    range: [0, 8]
required: []
descp:
  'Cropping layer for 1D input (e.g. temporal sequence).
   '

