constraints:
  data_format:
    default: channels_last
    dtype: tf.string
    enum:
    - channels_first
    - channels_last
  padding:
    default: valid
    dtype: tf.string
    enum:
    - same
    - valid
  pool_size:
    default: 2
    dtype: int
    structure: integer
    shape: 1
    range: [0, 6]
  strides:
    default: None
    dtype: int
    structure: integer
    shape: 1
    range: [0, 6]
required: []
descp:
  'Max pooling operation for 1D temporal data.'
