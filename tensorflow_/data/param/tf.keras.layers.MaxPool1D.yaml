constraints:
  data_format:
    default: channels_last
    dtype: tf.string
    enum:
    - channels_first
    - channels_last
  padding:
    default: valid
    dtype: tf.string
    enum:
    - same
    - valid
  pool_size:
    default: 2
    dtype: int
  strides:
    default: None
    dtype: int
descp:
  'Max pooling operation for 1D temporal data.'
