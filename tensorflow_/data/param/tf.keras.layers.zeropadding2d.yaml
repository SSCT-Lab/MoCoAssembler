constraints:
  data_format:
    default: None
    dtype: tf.string
    enum:
    - channels_first
    - channels_last
  padding:
    default: (1, 1)
    dtype: int
    structure:
    - integer
    - tuple
    - tuple_of_tuples
    shape: 2
    range: [0, 6]
required: []
descp:
  'Zero-padding layer for 2D input (e.g. picture).'
