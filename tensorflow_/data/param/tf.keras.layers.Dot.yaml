constraints:
  axes:
    default: (0, 0)
    dtype: int
    structure:
    - integer
    - tuple
    shape: 2
  normalize:
    default: False
    dtype: tf.bool
required:
  - axes
descp:
  'Layer that computes a dot product between samples in two tensors.'
