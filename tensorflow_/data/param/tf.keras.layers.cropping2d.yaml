constraints:
  cropping:
    default: ((0, 0), (0, 0))
    dtype: int
    structure:
    - tuple_of_tuples
    shape: 2
  data_format:
    default: None
    dtype: tf.string
    enum:
    - channels_first
    - channels_last
descp:
  'Cropping layer for 2D input (e.g. picture).'
