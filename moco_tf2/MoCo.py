from Tools.Assembler import Assembler
from Tools.Boundary import Boundary

# a = Assembler("lenet", experiment_name="test1_fuzzing", n=2)
# a.start()

# b = Boundary("lenet", experiment_name="test1_boundary")
# b.start()


# b = Boundary("lenet", experiment_name="test1_boundary", output_path="./output", fuzzing_path="test1_fuzzing")
# b.start2()
