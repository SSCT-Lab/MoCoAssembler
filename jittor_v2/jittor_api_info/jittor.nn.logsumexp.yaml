api: jittor.nn.logsumexp(x, dim, keepdims=False, keepdim=False)
constraints:
  dim:
    default: no default
    dtype:
    - int
  keepdim:
    default: 'False'
    dtype:
    - bool
  keepdims:
    default: 'False'
    dtype:
    - bool
descp: no description
extra_para: 1
inputs:
  optional:
  - keepdims
  - keepdim
  required:
  - dim
kind: api
