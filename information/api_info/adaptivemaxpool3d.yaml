function:
  torch: torch.nn.AdaptiveMaxPool3d
  ms: mindspore.nn.AdaptiveMaxPool3d
  paddle: paddle.nn.AdaptiveMaxPool3D
  jittor: jittor.nn.AdaptiveMaxPool3d
descp:
  torch: Applies a 3D adaptive max pooling over an input signal composed of several
    input planes.
  ms: Calculates the 3D adaptive max pooling for an input Tensor.
  paddle: This operation applies 3D adaptive max pooling on input tensor. The h and
    w dimensions of the output tensor are determined by the parameter output_size.
    The difference between adaptive pooling and pooling is adaptive one focus on the
    output size.
  jittor: no description
param: null
