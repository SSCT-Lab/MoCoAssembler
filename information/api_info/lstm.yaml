function:
  torch: torch.nn.LSTM
  ms: mindspore.nn.LSTM
  paddle: paddle.nn.LSTM
  tf: tf.keras.layers.LSTM
descp:
  torch: Applies a multi-layer long short-term memory (LSTM) RNN to an input sequence.
  ms: Stacked LSTM (Long Short-Term Memory) layers.
  paddle: Multilayer LSTM. It takes a sequence and an initial state as inputs, and
    returns the output sequences and the final states.
  tf: Long Short-Term Memory layer - Hochreiter 1997.
param: null
