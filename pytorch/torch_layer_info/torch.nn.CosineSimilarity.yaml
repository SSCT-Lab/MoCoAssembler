api: torch.nn.CosineSimilarity(dim=1, eps=1e-08)
constraints:
  dim:
    default: '1'
    descp: Dimension where cosine similarity is computed.
    dtype:
    - int
    range: null
    shape: null
    structure:
    - single
  eps:
    default: 1e-8
    descp: Small value to avoid division by zero.
    dtype:
    - float
    shape: null
    structure:
    - single
descp: "Returns cosine similarity between x1x_1x1\u200B and x2x_2x2\u200B, computed\
  \ along dim."
inputs:
  optional:
  - dim
  - eps
  required: []
