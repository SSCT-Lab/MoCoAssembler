api: torch.nn.GELU(approximate='none')
constraints:
  approximate:
    default: none
    descp: 'the gelu approximation algorithm to use: none | tanh.'
    enum: null
descp: 'Applies the Gaussian Error Linear Units function:'
inputs:
  optional:
  - approximate
  required: []
