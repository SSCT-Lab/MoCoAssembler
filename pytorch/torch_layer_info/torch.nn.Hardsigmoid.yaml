api: torch.nn.Hardsigmoid(inplace=False)
constraints:
  inplace:
    default: 'False'
    descp: can optionally do the operation in-place.
descp: Applies the Hardsigmoid function element-wise.
inputs:
  optional: []
  required:
  - inplace
