api: torch.nn.LazyLinear(out_features, bias=True, device=None, dtype=None)
constraints:
  bias:
    default: 'True'
    descp: If set to False, the layer will not learn an additive bias.
    dtype:
    - uninitializedparameter
  out_features:
    descp: size of each output sample
    dtype:
    - int
    range: null
    shape: null
    structure:
    - single
descp: A torch.nn.Linear module where in_features is inferred.
inputs:
  optional: []
  required:
  - out_features
  - bias
