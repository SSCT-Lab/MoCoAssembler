api: torch.nn.GLU(dim=- 1)
constraints:
  dim:
    default: '-1'
    descp: the dimension on which to split the input.
    range: null
    shape: null
    structure:
    - single
descp: "Applies the gated linear unit function GLU(a,b)=a\u2297\u03C3(b){GLU}(a, b)=\
  \ a \\otimes \\sigma(b)GLU(a,b)=a\u2297\u03C3(b) where aaa is the first half of\
  \ the input matrices and bbb is the second half."
inputs:
  optional: []
  required:
  - dim
