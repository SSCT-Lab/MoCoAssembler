# BUG LIST for TensorFlow

| Bug API                | Bug Type | File Path                                                    | Error Message                                                |
| ---------------------- | -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ActivityRegularization | BonBug   | [ActivityRegularization](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/ActivityRegularization/1.py) | The l1 and l2 parameters of the ActivityRegularization function are described in this document as floating point numbers and their values should be positive. |
| GRU                    | BonBug   | [GRU](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/GRU/1.py) | The document describes the dropout parameters of the GRU/LSTM/SimpleRNN function and the recurrent_dropout parameters are floating-point numbers with values ranging from 0 to 1, but the program can still run normally if the number is not between 0 and 1. |
| LSTM                   | BonBug   | [LSTM](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/LSTM/1.py) | The document describes the dropout parameters of the GRU/LSTM/SimpleRNN function and the recurrent_dropout parameters are floating-point numbers with values ranging from 0 to 1, but the program can still run normally if the number is not between 0 and 1. |
| SimpleRNN              | BonBug   | [SimpleRNN](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/SimpleRNN/1.py) | The document describes the dropout parameters of the GRU/LSTM/SimpleRNN function and the recurrent_dropout parameters are floating-point numbers with values ranging from 0 to 1, but the program can still run normally if the number is not between 0 and 1. |
| LeakyReLU              | BonBug   | [LeakyReLU](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/LeakyReLU/1.py) | The document describes the LeakyReLU function as having an alpha parameter whose type is a floating point number and the value is equal to or greater than 0. But we found that the value of this parameter is less than zero can also work. |
| Cropping2D/Cropping3D  | BonBug   | [Cropping2D](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/Cropping2D/1.py)、[Cropping3D](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/Cropping3D/1.py) | When the value type of Cropping in Cropping2D is int, tf doesn't check whether Cropping is positive or negative. |
| LayerNormalization     | BonBug   | [LayerNormalization](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/LayerNormalization/1.py) | Epsilon is documented as a small floating-point number.In our experiments, we found that epsilon works with larger floating-point numbers. |
| ZeroPadding1D          | ICBug    | [ZeroPadding1D](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/ZeroPadding1D/1.py) | ValueError: The `padding` argument must be a tuple of 2 integers. Received: {'padding': 1}. |
|                        | ICBug    |                                                              |                                                              |
| LayerNormalization     | ImpBug   | [LayerNormalization](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/LayerNormalization/2.py) | Epsilon is used to avoid division by zero errors, but when LayerNormalization takes a zero vector as input. |
| OOM                    | PerBug   | [OOM](https://github.com/SSCT-Lab/MoCoAssembler/blob/MoCo_1.0/bugs/tensorflow/OOM/1.py) | We have many similar model building and training code files, and train multiple models consecutively through one process. But in this process, we used the memory blank and the restriction function, and neither worked. |

