api: mindspore.nn.BiDense(in1_channels, in2_channels, out_channels, weight_init=None,
  bias_init=None, has_bias=True)
descp: The bilinear dense connected layer.
constraints:
  in1_channels:
    descp: in1_channels (int) – The number of channels in the input1 space.
    default: null
    dtype:
    - int
    structure:
    - single
    shape: null
    range: null
  in2_channels:
    descp: in2_channels (int) – The number of channels in the input2 space.
    default: null
    dtype:
    - int
    structure:
    - single
    shape: null
    range: null
  out_channels:
    descp: out_channels (int) – The number of channels in the output space.
    default: null
    dtype:
    - int
    structure:
    - single
    shape: null
    range: null
  weight_init:
    descp: 'weight_init (Union[Tensor, str, Initializer, numbers.Number]) – The trainable
      weight_init parameter. The values of str refer to the function initializer.
      Default: None .'
    default: None
    dtype:
    - union[tensor, str, initializer, numbers.number]
    - str
    enum: null
  bias_init:
    descp: 'bias_init (Union[Tensor, str, Initializer, numbers.Number]) – The trainable
      bias_init parameter. The values of str refer to the function initializer. Default:
      None .'
    default: None
    dtype:
    - union[tensor, str, initializer, numbers.number]
    - str
    enum: null
  has_bias:
    descp: 'has_bias (bool) – Specifies whether the layer uses (text{bias}) vector.
      Default: True .'
    default: 'True'
    dtype:
    - bool
inputs:
  optional: []
  required: []
