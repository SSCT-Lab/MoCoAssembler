api: mindspore.nn.MSELoss(reduction='mean')
descp: Calculates the mean squared error between the predicted value and the label
  value.
constraints:
  reduction:
    descp: 'reduction (str) – Type of reduction to be applied to loss. The optional
      values are "mean" , "sum" , and "none" . Default: ''mean'' .'
    default: '''mean'''
    dtype:
    - str
    enum: null
inputs:
  optional: []
  required: []
