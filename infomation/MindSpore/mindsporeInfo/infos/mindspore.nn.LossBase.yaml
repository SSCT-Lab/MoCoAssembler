api: mindspore.nn.LossBase(reduction='mean')
descp: Base class for other losses.
constraints:
  reduction:
    descp: 'reduction (str) – Type of reduction to be applied to loss. The optional
      values are "mean" , "sum" , and "none" . Default: "mean" .'
    default: '"mean"'
    dtype:
    - str
    enum: null
inputs:
  optional: []
  required: []
