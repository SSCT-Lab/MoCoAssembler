api: mindspore.nn.GetNextSingleOp(dataset_types, dataset_shapes, queue_name)
descp: Cell to run for getting the next operation.
constraints:
  dataset_types:
    descp: dataset_types (list[mindspore.dtype]) – The types of dataset.
    default: null
    dtype:
    - list[mindspore.dtype]
    structure:
    - list
  dataset_shapes:
    descp: dataset_shapes (list[tuple[int]]) – The shapes of dataset.
    default: null
    dtype:
    - list[tuple[int]]
    structure:
    - list
  queue_name:
    descp: queue_name (str) – Queue name to fetch the data.
    default: null
    dtype:
    - str
    enum: null
inputs:
  optional: []
  required: []
