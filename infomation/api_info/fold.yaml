function:
  torch: torch.nn.Fold
  paddle: paddle.nn.Fold
descp:
  torch: Combines an array of sliding local blocks into a large containing tensor.
  paddle: Combines an array of sliding local blocks into a large containing tensor.
    also known as col2im when operated on batched 2D image tensor. Fold calculates
    each combined value in the resulting large tensor by summing all values from all
    containing blocks.
param: null
