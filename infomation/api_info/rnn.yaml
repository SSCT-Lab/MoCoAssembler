function:
  torch: torch.nn.RNN
  ms: mindspore.nn.RNN
  paddle: paddle.nn.RNN
  tf: tf.keras.layers.SimpleRNN
descp:
  torch: "Applies a multi-layer Elman RNN with tanh\u2061\\tanhtanh or ReLU\\text{ReLU}ReLU\
    \ non-linearity to an input sequence."
  ms: Stacked Elman RNN layers.
  paddle: Wrapper for RNN, which creates a recurrent neural network with an RNN cell.
    It performs cell.forward() repeatedly until reaches to the maximum length of inputs.
  tf: Fully-connected RNN where the output is to be fed back to input.
param: null
